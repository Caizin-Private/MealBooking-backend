spring:
  datasource:
    url: jdbc:h2:mem:smart_workplace_test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:

  task:
    scheduling:
      enabled: false

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: false

office:
  latitude: 18.5204
  longitude: 73.8567
  radius-meters: 500
